import React, { useEffect } from 'react';
import { Skeleton, Spinner } from '@nexync/ui-web/components';

const Test = () => {
    useEffect(() => {}, []);
    return <div />;
};

export default Test;
