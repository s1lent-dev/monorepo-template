{
  "name": "@nexync/mongo",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsup --clean",
    "lint": "npx eslint . --ext .ts,.tsx"
  },
  "devDependencies": {
    "@nexync/lintconfig": "workspace:*",
    "@nexync/tsconfig": "workspace:*",
    "tsup": "^8.5.0",
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "mongoose": "^8.15.1"
  }
}
