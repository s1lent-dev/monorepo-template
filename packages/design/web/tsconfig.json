{
    "extends": "@nexync/tsconfig/tsconfig.next.json",
    "compilerOptions": {
        "rootDir": "src",
        "baseUrl": ".",
        "paths": {
            "@nexync/ui-web/*": ["src/*"]
        }
    },
    "include": ["src/**/*.ts", "src/**/*.tsx", "src/**/*.d.ts"],
    "exclude": ["node_modules"]
}
